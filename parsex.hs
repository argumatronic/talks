-- parsing examples: applicative/alternative, monadic

-- be sure to get slides, image files, and a top-level haskell .gitignore here before you build anythign with stack
-- be sure to put a separate .gitignore into the project just in fucking case